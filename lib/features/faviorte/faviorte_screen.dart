import 'package:flutter/material.dart';

class FaviortScreen extends StatelessWidget {
  const FaviortScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Faviort Page')),
    );
  }
}
